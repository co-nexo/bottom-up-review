---
name: bottom-up-review
description: "沿变更代码的调用链自底向上追踪业务流程，并根据任务目标审阅 Git 差异。适用于聚焦具体变更的代码审阅；不适用于从零实现功能或通用架构讲解。"
---

# 自底向上审阅

围绕变更审阅能够说明问题的最小业务切片。从具体实现改动出发，沿调用方和业务流程向上追踪，最后检查任务目标是否真正达成。审阅应以证据为依据，并保持简洁。

## 范围与证据

### 选择审阅范围

按照以下优先级选择最小且可靠的边界：

1. 用户明确提供的文件列表、差异、提交或版本范围。
2. 实现或发布流程移交的提交 SHA 或版本范围。
3. 如果工作区干净且没有移交范围，将最新提交 `HEAD^..HEAD` 作为候选范围。审阅前检查它的提交主题、变更文件和任务目标是否一致；“最近的提交”仅指这个明确边界，不是任意时间窗口。如果 `HEAD` 没有父提交，则使用根提交作为候选范围。
4. 如果工作区存在未提交变更，审阅 `git diff HEAD` 以及相关的未跟踪文件，不要悄悄将其与 `HEAD^..HEAD` 合并。如果已提交和未提交的变更可能都属于当前任务，应询问审阅范围，或将范围标记为无法确定。

始终说明选定的范围，包括提交 SHA、提交主题或工作区状态。提交信息可以作为判断任务是否匹配的证据，但不能单独作为扩大范围的依据。遇到合并提交、多个提交，或最新提交的文件与任务目标不符时，不要猜测；询问预期的起点和终点，或将结论标记为 `无法确认`。

实现类 Skill 最可靠的移交信息是它产生的精确提交 SHA 或范围，例如 `abc1234^..abc1234`。没有这类移交信息时，只有在输出中展示范围检查结果后，才可以使用上述单提交回退方案。

- 不要修改工作区。如果没有可用的仓库、版本范围或差异，应报告这一事实，不要进行推测性审阅。
- 将当前请求、Issue/PR 描述以及用户明确提供的其他要求作为任务目标，不要仅凭代码虚构目标。如果目标缺失或存在实质歧义，应说明限制；只有在继续审阅会导致结论不可靠时，才提出一个简洁的问题。
- 通过仓库命名、包边界、测试、契约、Schema 和入口点识别业务上下文。不要把无关改动强行归入同一条业务流；必要时拆分为多条业务流。
- 所有判断必须基于代码证据。非显而易见的结论标记为 `[推断]`，无法确定的契约或行为标记为 `[无法确认]`。不得将推断出的业务规则陈述为已观察到的事实。

## 业务流分组与固定审阅层级

先将变更划分为业务流，再使用相同的四层框架展示每条业务流。不要把业务流边界、包边界和审阅层级混为一谈。

最小业务流定义为：

```text
相同的业务触发 + 相同的核心状态变化 + 相同的可观察结果
```

- 只有属于同一业务闭环的变更符号才能归为一组。仅仅共用 Repository 或工具类，不足以构成同一条业务流。
- 如果一个被修改的底层符号存在不同业务意图的调用方，应沿调用链向上追踪，直到业务意图、核心状态变化或结果开始分化，并在此处拆分业务流。
- 如果多个入口触发相同的业务意图和状态变化，则保留为同一条业务流，并在业务场景层列出这些入口。
- 如果无法根据代码和已提供目标证明业务边界，应标记为 `[推断]` 或 `[无法确认]`，不要虚构业务名称。

每条业务流使用以下四个语义层级。它们是审阅视角，并不要求仓库必须采用这些包名。应把框架特有的类映射到最接近的层级，而不是直接用 `controller/service/repository` 作为审阅结构。

| 自底向上层级 | 主要回答的问题 | 常见证据 |
| --- | --- | --- |
| 数据层 | 读写了什么数据，外部交互或数据契约发生了什么变化？ | DAO、Repository、Mapper、Gateway、缓存、RPC、消息、SQL、序列化 |
| 领域状态层（聚合层） | 哪个业务对象或状态发生了什么变化，维护了哪些不变量？ | Aggregate、Entity、Value Object、Domain Policy、状态迁移 |
| 用例编排层 | 如何组织多个对象或步骤，顺序、事务、重试、幂等和权限怎样影响流程？ | Use Case、Application Service、Domain Service、事务脚本 |
| 业务场景层（结果层） | 改变了哪个业务场景，调用方最终观察到什么结果？ | API、事件、任务、Facade、响应以及任务目标 |

- 每条链固定按照“数据层 → 领域状态层 → 用例编排层 → 业务场景层”展示。某层未参与变更时写 `未涉及`，不要依靠猜测补齐；如果项目没有显式的聚合行为，写 `未发现独立聚合行为`，不要把普通数据对象强行称为聚合。
- 一处代码如果横跨多个角色，只选择对业务因果关系最重要的主层级，次要作用写在同一行，不要新增第五层。只有业务意图、核心状态变化或最终结果确实独立时，才拆分业务流。
- “业务场景层”表示业务意图和可观察结果，不等同于 Controller，也不要求业务规则必须位于名为 `Business` 的类中。

## 审阅方法

1. 阅读差异，定位被修改的符号或配置项。对每一项变更，先确认它产生的直接行为，再作判断。
2. 根据“触发—状态变化—结果”规则，将变更符号划分为一条或多条最小业务流，再把每条业务流的证据放入上述四个固定层级。沿调用方和数据/状态流向上追踪，只追踪到足以把变更点连接到业务场景的位置。
3. 对每个实际涉及的层级只回答三个问题：改了什么、产生了什么直接行为、它启用或改变了什么上层行为。当业务动作和可观察结果已经明确时停止，不要绘制整个仓库。
4. 只检查可能改变结论的上下文：直接调用方和被调用方、数据或序列化契约、事务和错误边界、授权、缓存、幂等、并发行为、相关测试，以及 Schema、SQL、配置和事件定义。只检查与当前变更相关的事项。
5. 将还原出的行为与每一项明确目标进行比较。只有当问题有具体位置作为证据，并且可能影响正确性、任务目标、数据完整性或重要的可观察契约时，才报告为风险。不要把输出篇幅浪费在格式偏好或通用最佳实践上。

## 输出格式

以结论开头，并让整份审阅能够在一次浏览中读完。

```text
结论：<达成 | 部分达成 | 未达成 | 无法确认> — <一句话原因>
范围：<版本范围或工作区；如有不确定性一并标出>

变更链：<业务流名称；按触发—核心状态变化—结果命名>
| 层级 | 证据节点 | 改了什么 → 直接效果 | 对上层/业务结果的影响 |
| 数据层 | ... | ... | ... |
| 领域状态层 | ... | ... | ... |
| 用例编排层 | ... | ... | ... |
| 业务场景层 | ... | ... | ... |

目标核对：
- <目标>：<对应证据或缺口> → <达成 | 部分达成 | 未达成 | 无法确认>

风险（仅在存在具体问题时）：
- [阻断 | 重要 | 一般] `path/to/file:line`：<问题>；影响：<后果>；建议：<最小修正方向>
```

- `变更链` 应先按照业务流拆分，再严格使用四个固定层级，并按照数据层、领域状态层、用例编排层、业务场景层的顺序排列。证据节点使用 `path:line` 或 `Class#method`，每一层只保留新增的因果信息。
- `范围` 放在结论之后，让审阅者先看到判断，再快速确认是否审对了代码。如果范围本身不可靠，结论不得伪装成确定结论。
- `目标核对` 只映射任务目标与证据，不重复整段变更链。没有明确目标时写明“目标未提供，无法判定是否达成”，但仍可进行行为审阅。
- 没有具体风险时写“未发现会阻断目标的具体问题”，不要罗列空泛的审阅建议。发现问题时，按照对任务目标和数据的实际影响排序。
- 结论规则：所有目标都有代码证据且不存在阻断问题时为“达成”；核心路径存在但仍有目标缺口或重要边界未覆盖时为“部分达成”；行为与目标冲突或核心路径失效时为“未达成”；缺少目标、调用契约或运行时事实，因而无法判断时为“无法确认”。

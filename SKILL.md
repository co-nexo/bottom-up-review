---
name: bottom-up-review
description: "Review Git diffs bottom-up through the affected business flow and verify the stated task goal. 用于聚焦具体变更的业务审阅；不适用于功能实现或通用架构讲解。"
---

# 自底向上审阅

从 Git 变更出发，只追踪能够解释业务影响的最小代码切片，并用代码证据核对任务目标。

## 范围

按以下优先级选择最小可靠范围：

1. 用户明确提供的文件列表、差异、提交或版本范围。
2. 上游流程移交的精确提交 SHA 或范围。
3. 工作区有未提交变更时，审阅 `git diff HEAD` 和相关未跟踪文件，不与最近提交混合。
4. 工作区干净且没有指定范围时，将 `HEAD^..HEAD` 作为候选；先核对提交主题、文件和任务目标。根提交使用其自身范围。

如果已提交和未提交变更可能属于同一任务，或候选提交是合并提交、包含多个提交、与目标不符，不要猜测范围；询问用户或标记为 `[无法确认]`。始终报告实际审阅范围。没有仓库或差异时直接说明，不修改工作区。

任务目标仅来自用户请求、Issue/PR 描述或明确移交信息。结论必须有代码证据；非显而易见的判断标记为 `[推断]`，缺少契约或运行时事实时标记为 `[无法确认]`。

## 上下文限制

- 先读 diff 和变更符号附近的局部代码，不默认读取完整文件。
- 默认只检查直接调用方、被调用方及相关测试或契约；只有无法识别业务触发、状态变化或结果时才继续一层。
- 可阅读测试代码，但不重复运行测试、构建或静态检查；复用实现阶段或 CI 已提供的结果，仅在用户明确要求时执行。
- 同类调用方选择能证明行为的代表性入口；只有行为不同才分别展开。
- 只检查可能改变结论的事务、错误、权限、缓存、幂等、并发、Schema、SQL、配置、序列化或事件定义。
- 一旦能够说明“触发 → 核心状态变化 → 可观察结果”并判断目标，即停止追踪。
- 预计超过 20 个文件或 3 条业务流时，拆分审阅并说明当前批次，不一次加载全部上下文。

## 审阅

1. 从 diff 定位变更符号，确认每项改动的直接行为。
2. 按“相同触发 + 相同核心状态变化 + 相同可观察结果”划分最小业务流。共用 Repository 或工具类不足以归为同一流；业务意图分化时拆分。
3. 沿调用和数据流向上追踪，将证据映射到固定四层：

| 层级 | 核心问题 |
| --- | --- |
| 数据层 | 数据、外部交互或契约如何变化？ |
| 领域状态层 | 哪个业务状态变化，哪些不变量受影响？ |
| 用例编排层 | 顺序、事务、重试、幂等或权限如何影响流程？ |
| 业务场景层 | 调用方最终观察到什么结果？ |

每条业务流按上述顺序展示。未参与的层写 `未涉及`；没有独立聚合行为时写 `未发现独立聚合行为`，不要强行补齐层级或虚构业务名称。

4. 对照每项明确目标，只报告有具体位置且影响正确性、目标、数据完整性或重要可观察契约的问题。忽略格式偏好和空泛最佳实践。

## 输出

```text
结论：<达成 | 部分达成 | 未达成 | 无法确认> — <一句话原因>
范围：<版本范围或工作区；含不确定性>

变更链：<触发—核心状态变化—结果>
| 层级 | 证据节点 | 改了什么 → 直接效果 | 对业务结果的影响 |
| 数据层 | ... | ... | ... |
| 领域状态层 | ... | ... | ... |
| 用例编排层 | ... | ... | ... |
| 业务场景层 | ... | ... | ... |

目标核对：
- <目标>：<证据或缺口> → <结论>

风险：
- [阻断 | 重要 | 一般] `path:line`：<问题>；影响：<后果>；建议：<最小修正>
```

没有明确目标时，说明无法判定目标是否达成，但仍可审阅行为。没有具体风险时写“未发现会阻断目标的具体问题”。风险按实际影响排序。

结论规则：目标均有证据且无阻断问题为 `达成`；存在目标缺口或重要边界未覆盖为 `部分达成`；行为与目标冲突或核心路径失效为 `未达成`；范围、目标、契约或运行时事实不足为 `无法确认`。

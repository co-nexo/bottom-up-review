---
name: bottom-up-review
description: "Review Git diffs by finding each business flow's lowest explanatory change, tracing only the semantic transitions that affect use-case behavior and business outcomes, and verifying the stated task goal. 用于聚焦具体变更的业务审阅；不适用于功能实现或通用架构讲解。"
---

# 自底向上业务审阅

从 Git 变更中识别每条业务流实际变化的最低事实，只沿改变业务语义的路径向上追踪，验证底层实现是否形成正确的用例行为和外部结果。层级只是证据标签，不是必须走完的架构清单。

## 范围

- 按“用户指定范围 → 上游移交的 SHA/范围 → `git diff HEAD` 与未跟踪文件 → 干净工作区的 `HEAD^..HEAD` 候选”选择最小可靠范围；根提交使用自身范围。
- 已提交与未提交变更可能属于同一任务、候选是合并提交或目标不符时，不猜测；询问用户或标记 `[无法确认]`。
- 任务目标只来自用户请求、Issue/PR 或明确移交信息。没有目标仍可说明行为，但不能判断是否达成。
- 先冻结审阅范围，再创建报告文件，避免报告进入本次 diff。没有仓库或差异时直接说明。
- 先读 diff，只扩展到证明触发、状态变化和结果所需的直接调用方、被调用方、测试或契约。
- 可阅读测试，但不重复运行测试、构建或静态检查；仅复用明确提供的实现阶段或 CI 结果，除非用户要求执行。
- 预计超过 20 个文件或 3 条业务流时拆批，并说明当前批次。

## 证据类型

按变更片段的实际职责分类，不按目录、类名或架构风格分类：

| 类型 | 职责 |
| --- | --- |
| Data | 持久化结构、字段、约束、索引及数据迁移 |
| Adapter | SQL、ORM、缓存、序列化或外部系统的读写与转换 |
| Port | 核心拥有的 Repository、Gateway、Clock、Publisher 等能力契约 |
| Domain | 业务规则、不变量、计算和状态迁移 |
| Use Case | 业务动作、调用顺序、事务、权限、幂等和并发编排 |
| Entry | HTTP、事件、任务、CLI 等外部触发及可观察契约 |

每个证据节点必须标注且只能标注一个规范类型：`Data`、`Adapter`、`Port`、`Domain`、`Use Case` 或 `Entry`。类型按本次变更片段的实际职责判断，不按目录、类名或架构层级套用。Data 不包括 SQL/ORM Mapper；Domain 不由 `Entity` 类名决定；Port 由契约所有权决定。项目没有独立 Domain 或 Port 时直接省略。同一文件可提供多类证据；一个符号混合职责时，选择最能解释本次变化的类型并说明合并职责，不拆出虚构节点。

## 证据类型展示约束

每个证据节点只展示能够解释本次业务变化的字段，不完整复述代码结构。所有节点共享以下基础信息：

| 基础字段 | 展示要求 |
| --- | --- |
| 位置 | `path:line · symbol` |
| 变化 | `[变更]` 写 `before → after`；`[承接]` 明确写“未改动” |
| 业务含义 | 直接改变了什么事实、规则、行为或外部结果 |
| 传导 | 只有存在下一证据节点时展示；明确字段、状态、结果或调用如何向上传递 |

除基础信息外，按类型选择字段：

| 类型 | 必须展示 | 按需展示 | 不应重点展示 |
| --- | --- | --- | --- |
| Data | 数据对象、字段/约束变化、允许或禁止的数据状态 | 默认值、可空性、唯一性、外键、索引、迁移兼容性 | Java 实体字段、Mapper 实现细节 |
| Adapter | 输入来源、读写/转换行为、输出语义 | SQL 条件、排序、锁、缓存、序列化、外部错误转换 | 无语义的字段逐一映射 |
| Port | 能力名称、输入契约、输出契约 | 错误语义、调用约束、契约所有权变化 | 接口方法签名的机械变化 |
| Domain | 输入事实、业务规则/不变量、产生的状态或计算结果 | 决策条件、状态迁移、拒绝原因、边界值 | getter/setter、领域对象结构本身 |
| Use Case | 触发条件、关键业务步骤、最终结果 | 调用顺序、事务、幂等、权限、并发、补偿 | 普通方法调用链和机械编排 |
| Entry | 外部触发、请求/事件契约、可观察结果 | HTTP 状态码、错误码、鉴权、兼容性、版本变化 | Controller 内部转调细节 |

字段选择遵守：`影响业务结论 > 证明语义连续性 > 帮助理解实现 > 代码完整性`。一个字段不影响业务含义就不展示；相邻节点已证明的信息不重复展示；`before → after` 只在变化首次发生的节点展示一次；一个节点通常控制在 2～4 个业务字段，超过 4 个时只保留影响目标核对或风险判断的字段；`[承接]` 节点原则上比 `[变更]` 节点更短。

无法用该类型的必需字段说明业务作用时，不展示这个节点。每个节点都应能压缩为：哪里变了 → 哪个业务事实因此改变 → 如何影响下一层或最终结果。

## 审阅方法

1. 按“相同触发 + 相同核心状态变化 + 相同可观察结果”划分最小业务流。
2. 为每条业务流选择 diff 中最低且足以解释变化来源的节点；迁移从 Data、SQL/Mapper 从 Adapter、规则从 Domain、编排从 Use Case、纯协议变化从 Entry 开始。
3. 从起点只追踪改变业务含义或证明连续性的节点；纯 DTO、机械透传和无语义 Mapper 可压缩，字段漏传、查询条件、锁、错误、事务或返回契约会改变结论时必须展开。
4. 连接不上时报告断链或标记 `[无法确认]`；能说明“底层事实 → 业务语义 → 用例行为 → 可观察结果”后停止。
5. 对照每项目标判断完成度，只报告影响正确性、目标、数据完整性或重要外部契约的问题。

证据节点规则：

- `[变更]` 必须位于 diff，并写明语义上的 `before → after`；Schema 字段和约束逐项说明。
- `[承接]` 表示符号未修改但承接了变更输入，必须明确写“未改动”，不能伪装成本次成果。
- 第一个节点必须是 `[变更]`；每个节点只引用一个可独立核查的 `path:line · symbol`，优先使用可点击的绝对路径源码链接。
- 相邻节点必须换行，用独立的 `↓ 传导：...` 指出具体字段、状态、结果或调用如何进入下一节点；不为凑层级添加节点。
- 风险必须给出位置、触发条件、后果、最小修正，并指出它破坏的业务流、业务语义或目标。非显而易见的判断标记 `[推断]`。

## 输出

生成独立 Markdown 报告。用户指定路径时写入该路径；用户要求“放在本地仓库但不提交”时，优先写入仓库内的 `docs/reviews/`，并将精确文件路径加入当前仓库的 `.git/info/exclude`。只修改本地排除配置，不修改或新增 `.gitignore`；写入前保留已有规则，避免重复追加。若用户未指定保存位置，也未要求仓库内保存，写入系统临时目录，命名为 `bottom-up-review-<repo>-<scope>.md`。无法创建文件时才回退为聊天内 Markdown。聊天回复不超过 5 行，只给总体结论、风险数量、验证状态和报告链接。

报告在标准 Markdown 兼容范围内尽可能增强可读性，不依赖颜色、内嵌 CSS、脚本或渲染器专属扩展。优先使用以下表达组合：

- 开头使用一个简短标题和“结论 / 范围 / 风险 / 验证”摘要表，让读者先看到结果。
- 用 GFM 表格表达状态、字段映射、HTTP 契约、风险清单和 before → after 对比；单元格内容保持短句，复杂解释移到正文。
- 用 `> [!TIP]`、`> [!WARNING]`、`> [!CAUTION]`、`> [!NOTE]` 表达结论和风险；同时保留文字标签，不依赖颜色才能理解。
- 用 `🟢`、`🟡`、`🔴`、`⚪` 作为状态辅助标识，但不得让图标成为唯一语义。
- 用有序列表表达业务顺序；每个节点结束后另起一行，用缩进箭头表达因果链：

  ```markdown
  1. `[变更][类型：Data]` 节点一
     ↓ 传导：字段 `x` 进入下一节点
  2. `[承接][类型：Adapter]` 节点二
  ```

  需要展示分支时使用 Mermaid 以外的纯文本分支，保证普通 Markdown 也可读。
- 使用短横线、分隔线和适量三级标题建立层级；避免连续堆叠粗体、全大写标题、装饰性 ASCII 框和重复结论。
- 源码链接使用可点击的绝对路径；首次出现的专业缩写给出全称；代码片段只保留能证明语义的最小片段。
- 表格、列表、标题和 callout 之间保留空行；每个段落只承担一个判断，优先短句。

推荐的报告骨架是：

```markdown
# 自底向上业务审阅

| 项目 | 结果 |
| --- | --- |
| 总体结论 | 🟡 部分达成 |
| 审阅范围 | `git diff HEAD` + 未跟踪文件 |
| 重要风险 | 1 高 / 1 中 |
| 验证状态 | 未运行测试、构建或静态检查 |

## 总体结论与范围
...

## 业务结果总览
...

## 业务流：...
...

## 风险
...

## 验证说明
...
```

硬限制：

- 单文件不超过 180 个非空行和 16 KiB。
- 单文件最多 3 条业务流；每条最多 4 个证据节点。
- 单文件最多 5 项风险；超限时按业务流拆分，不截断阻断或重要风险。
- 写完后检查 UTF-8 字节数和非空行数，超限则压缩重复表达或拆分后重新检查；不能用 `<details>` 规避限制。

报告只保留：总体结论与范围、业务结果总览、各业务流的业务变化/证据链/局部结论、目标核对、风险和验证说明。

按证据职责选择一种主要 Markdown 形式，并优先回答对应问题：

| 类型 | 首要回答的问题 | 推荐形式 |
| --- | --- |
| Data | 数据现在能表达什么？ | 字段/约束表 |
| Adapter | 数据如何被正确读写/转换？ | 语义 `diff` |
| Port | 核心依赖的能力契约是什么？ | I/O 契约表 |
| Domain | 什么业务规则决定结果？ | 决策表/状态转换 |
| Use Case | 一次业务动作如何完成？ | 关键流程 |
| Entry | 外部最终看到什么？ | API/Event 契约 |

补充约束：Data 重点回答“存什么变了、数据库现在允许/拒绝什么、是否影响已有数据或兼容性”；Adapter 只保留影响语义的 `WHERE`、`ORDER BY`、`JOIN`、锁和关键映射；Port 只有本身发生语义变化或理解断链所必需时才单独展示；Domain 必须存在实际规则，不因 `Entity` 或 `Aggregate` 命名自动添加；Use Case 只在正确性受影响时展开事务、幂等、并发、权限或补偿；Entry 涉及必填字段、状态码、错误码、鉴权或版本变化时说明兼容性影响。

一个节点只使用一种视觉形式，不重复同一个 `before → after`，代码片段不超过 8 行。业务流结论与风险可使用 GFM callout：达成 `TIP`、部分达成或重要风险 `WARNING`、未达成或阻断风险 `CAUTION`、无法确认或一般风险 `NOTE`。图标只用于区分证据类型，不在正文堆叠。

结论规则：目标均有证据且无阻断问题为 `达成`；存在目标缺口或重要边界未覆盖为 `部分达成`；行为与目标冲突或核心路径失效为 `未达成`；范围、目标、契约或运行时事实不足为 `无法确认`。

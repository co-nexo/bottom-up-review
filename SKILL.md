---
name: bottom-up-review
description: "Review recent concentrated code changes around one concrete business capability by reconstructing the implemented behavior bottom-up and verifying only the technical guarantees that behavior requires. 用于聚焦具体业务变更的审阅；不适用于功能实现或通用架构讲解。"
---

# 自底向上业务审阅

针对近期围绕一个具体业务形成的集中代码变化，先还原实际业务实现，再从业务结果反向验证必要的技术保证。层级只是证据标签，不是必须逐层经过的架构清单。

## 核心边界

- 一次默认只审阅一个具体业务；独立业务不得合并。共享代码、DTO、表或基础设施本身不是合并依据，只有最终业务结果相同的关联变化才可合并。
- “近期”表示围绕同一业务的连续变更，不按固定天数截断。
- Git diff、commit range 和未跟踪文件是业务范围的证据，不直接定义业务范围。
- 目标只来自用户请求、Task、Spec、Issue、PR 或明确移交信息；没有目标时只描述实际行为、缺口和风险，不判断是否达成。
- 没有 Git 仓库或没有可审阅差异时，直接说明原因。
- 先冻结范围，再创建报告；默认不运行测试、构建或静态检查，除非用户明确要求。

## 工作流

### 1. Business Scope Discovery（轻量）

开始时只看：working tree 状态、`git diff`/未跟踪文件、近期 commit message、变更路径/文件名、diff summary 和少量关键符号。此阶段不得深入调用链、业务规则或技术保证；调用链从第二阶段开始。

按“相同触发 + 相同核心状态变化 + 相同可观察结果”归并，识别 1～3 个候选业务。每个候选必须能用一句话描述：

> 系统现在能够/不能够……

用户已指定或只有一个连贯候选时，自动选定，不提问。若存在多个候选，即使它们共享基础设施，也不得据此自动合并；只有业务结果明确相同才可视为同一业务，否则询问用户。

存在多个独立候选且用户未指定时，最多询问 3 个问题，并优先使用选择题：

1. 本次具体审阅哪个业务？
2. 是否纳入关联的 working tree 变更和近期 commits？
3. 没有明确目标时，选择：A. 只分析实际行为和风险；B. 按以下验收目标判断：`____`。

有没有 Task/Spec/Issue/PR 应自动从上下文和仓库信息判断，不单独询问。未获得范围确认时，不擅自合并业务，标记 `[无法确认]`。

### 2. 冻结范围并建立证据

- 记录选定业务、包含/排除的 commits 和文件、范围依据及目标来源。
- 冻结后才深入直接调用方、被调用方、测试和契约；只扩展到证明触发、状态变化、结果或风险所需的节点。
- 从 diff 中选择最低且足以解释业务变化的 `[变更]` 节点。

### 3. Bottom-up

从最低有效技术事实向上追踪，直到能够说明“技术事实 → 业务语义 → 用例行为 → 外部结果”。选择起点时以下优先级仅作判断辅助，不是机械层级：

`数据约束/状态变化 > 领域规则变化 > 业务编排变化 > 入口变化`

优先选择首次改变业务不变量、状态迁移或数据约束的位置，而不是单纯新增入口的位置；若实际语义首次改变在查询条件、锁或转换逻辑，则从对应 Adapter 起点。证据标签只能使用一种：

| 标签 | 关注内容 |
| --- | --- |
| Data | 字段、约束、索引、迁移及可表达的数据状态 |
| Adapter | SQL/ORM、缓存、序列化、外部系统读写和转换 |
| Port | Repository、Gateway、Clock、Publisher 等能力契约 |
| Domain | 业务规则、不变量、计算和状态迁移 |
| Use Case | 业务动作、事务、幂等、权限、并发和补偿编排 |
| Entry | HTTP、事件、任务、CLI 触发及外部契约 |

不要按目录、类名或架构层级分类。没有独立 Domain/Port 时省略；混合职责时选择最能解释本次变化的标签。纯 DTO 透传、getter/setter 和无语义映射不展开。

Bottom-up 只输出：

1. **起点 / 最低有效技术事实**：位置、语义变化、业务事实，以及“为什么从这里开始”。说明该节点首次改变了什么，上游只是触发还是下游只是承接。
2. **技术实现链**：使用 `A → B → C`，只保留改变业务语义的节点；不堆叠纵向“↓ 传导”。
3. **Business Delta**：明确 `Before → After` 的业务变化。
4. **Business Implementation**：明确触发、状态变化、成功/失败结果及实际形成的业务行为。

节点要求：`[变更]` 必须位于 diff，并写语义上的 `before → after`；`[承接]` 必须明确“未改动”。每个节点使用 `path:line · symbol`，只展示影响结论的字段和证据。

### 4. Technical Verification（按需）

从 Business Implementation 反推该业务成立所依赖的技术保证，只检查实际依赖的项目，不把清单全部执行一遍。例如纯查询通常不检查幂等、补偿或事务；只有业务行为依赖时才检查事务、幂等、约束、状态一致性、不变量、并发、权限、补偿或绕过路径。

每项验证给出代码位置、结论和缺口。风险说明位置、触发条件、后果、影响的业务语义及最小修正；非显而易见的判断标记 `[推断]`。

### 5. 结论

- 目标均有证据且无阻断问题：`达成`。
- 存在目标缺口或重要边界未覆盖：`部分达成`。
- 行为与目标冲突或核心路径失效：`未达成`。
- 范围、目标、契约或运行时事实不足：`无法确认`。

## 报告输出

范围冻结后生成独立 Markdown 报告。按以下规则解析路径和文件名，不自行改变目录或命名格式。

- 默认：`<git-root>/tmp/bottom-up-review/`
- 用户明确要求持久化时：`<git-root>/docs/reviews/`
- 禁止系统临时目录、`%TEMP%` 和用户 HOME。
- 文件名：`bottom-up-review-{repo}-{scope}.md`
- `repo` 为 Git 根目录名的小写短横线形式；`scope` 优先使用用户提供的业务/Issue 标识，否则使用选定 commits 与变更路径排序后的确定性短哈希。
- 如需避免报告进入 diff，只将精确路径加入 `.git/info/exclude`，不修改 `.gitignore`。

报告使用标准 Markdown，不依赖 CSS、脚本或颜色。默认只展示一个 Business Scope，通常只展示其核心 Flow。只有用户明确要求合并多个、且最终业务结果相同的关联业务时，才最多展示 3 个 Business Flow；每个最多 5 个关键证据、最多 5 项风险。复杂业务不得为满足数量限制删除阻断证据。

报告顶部必须给出审阅置信度：`高`表示范围清楚、实现链完整且有关键契约或测试等直接证据；`中`表示主要依赖静态代码推断或缺少运行验证；`低`表示范围、契约或关键实现事实仍不清楚。

## Markdown 模板

```markdown
# 自底向上业务审阅

## Summary

| 项目 | 结果 |
| --- | --- |
| 结论 | 达成 / 部分达成 / 未达成 / 无法确认 |
| 置信度 | 高 / 中 / 低 |
| Business Scope | 系统现在能够/不能够…… |
| Git Evidence | commits、working tree、文件范围 |
| 风险 | 高 / 中 / 低 |
| 验证 | 未运行 / 已有结果 / 已检查代码 |

## Business Scope

- 选定业务：
- 候选及排除项：
- 范围依据：
- Task / Spec / Issue：未提供则写明“不判定目标达成度”

## Bottom-up

### 起点 / 最低有效技术事实

`[变更][类型：Data|Adapter|Port|Domain|Use Case|Entry]` path:line · symbol

- 语义变化：
- 业务事实：
- 选择原因：为什么这里是最低有效节点；上游/下游分别承担什么角色

### 技术实现链

`证据 A → 证据 B → 证据 C`

### Business Delta

**Before**：

**After**：

### Business Implementation

- 触发条件：
- 核心状态变化：
- 成功/失败及外部结果：

## Technical Verification

只列 Business Implementation 实际依赖的保证。

| 必要保证 | 代码证据 | 结论 | 缺口 |
| --- | --- | --- | --- |
| 与 Business Implementation 直接相关的保证 | path:line | 通过/缺失/无法确认 | ... |

## Details / Evidence（按需）

- 目标核对：
- 关键风险：
- 验证说明：
```

---
name: bottom-up-review
description: "Review Git diffs by finding each business flow's lowest explanatory change, tracing only the semantic transitions that affect use-case behavior and business outcomes, and verifying the stated task goal. 用于聚焦具体变更的业务审阅；不适用于功能实现或通用架构讲解。"
---

# 自底向上审阅

从 Git 变更中先识别每条业务流实际发生变化的最底层，再从该层的具体字段、函数、契约或规则变化出发，只沿改变业务语义的实际路径向上追踪，验证底层实现是否支撑顶层业务设计。目标是形成“底层事实 → 关键语义 → 用例行为 → 顶层业务结果”的短证据链，而不是逐层检查或复述系统架构。

## 范围

按以下优先级选择最小可靠范围：

1. 用户明确提供的文件列表、差异、提交或版本范围。
2. 上游流程移交的精确提交 SHA 或范围。
3. 工作区有未提交变更时，审阅 `git diff HEAD` 和相关未跟踪文件，不与最近提交混合。
4. 工作区干净且没有指定范围时，将 `HEAD^..HEAD` 作为候选；先核对提交主题、文件和任务目标。根提交使用其自身范围。

如果已提交和未提交变更可能属于同一任务，或候选提交是合并提交、包含多个提交、与目标不符，不要猜测范围；询问用户或标记为 `[无法确认]`。始终报告实际审阅范围。没有仓库或差异时直接说明，不修改工作区。

任务目标仅来自用户请求、Issue/PR 描述或明确移交信息。结论必须有代码证据；非显而易见的判断标记为 `[推断]`，缺少契约或运行时事实时标记为 `[无法确认]`。

## 上下文限制

- 先读 diff 并为变更符号判定证据类型，找出每条业务流的最低变化来源；不默认从表结构或完整文件开始。
- 默认只检查直接调用方、被调用方及相关测试或契约；只有无法识别业务触发、状态变化或结果时才继续一层。
- 可阅读测试代码，但不重复运行测试、构建或静态检查；复用实现阶段或 CI 已提供的结果，仅在用户明确要求时执行。
- 同类调用方选择能证明行为的代表性入口；只有行为不同才分别展开。
- 只检查可能改变结论的事务、错误、权限、缓存、幂等、并发、Schema、SQL、配置、序列化或事件定义。
- 一旦能够说明“底层事实 → 关键语义 → 用例行为 → 可观察结果”并判断目标，即停止追踪。
- 预计超过 20 个文件或 3 条业务流时，拆分审阅并说明当前批次，不一次加载全部上下文。

## 审阅

1. 从 diff 定位变更符号，并按职责判定其证据类型。类型用于解释证据，不代表必须逐层输出；不要仅凭文件名或包名分类。

| 证据类型 | 职责边界与常见证据 | 重点审什么 | 不应归入 |
| --- | --- | --- | --- |
| Data | 持久化数据能够存储和约束什么；迁移、DDL、表、列、集合 Schema、持久化枚举、索引、外键、检查约束及存量数据迁移 | 字段或约束的增加、删除、类型、默认值、可空性、取值范围、唯一性、引用完整性及 `before → after` | SQL 查询、ORM 映射、Repository 实现；API DTO；领域不变量 |
| Adapter | 核心与数据库、缓存、消息系统或第三方服务之间的技术适配；SQL、ORM/DAO/Mapper、Repository/Gateway 实现、序列化器、客户端和持久化模型转换 | 数据如何查询、写入、锁定、聚合和转换；Data 字段是否完整进入或离开核心契约；并发与错误如何翻译 | 业务决策、领域不变量；核心定义的 Port 接口；Controller 的入站协议处理 |
| Port | 核心拥有的能力边界；主链默认指 Repository、Gateway、Clock、EventPublisher 等被驱动端接口。显式 Use Case 输入接口属于 Entry 与 Use Case 之间的驱动端 Port | 方法参数、返回类型、错误、原子性和语义契约；Adapter 是否正确实现；Use Case 在什么条件下调用 | Port 实现及技术细节；仅因类名含 `Repository` 或 `Service` 就归类 |
| Domain | 业务含义和规则；Aggregate Root、Entity、Value Object、Domain Service、Policy/Specification、Factory、Domain Event、领域错误 | 状态、不变量、行为、决策规则、状态迁移、领域计算和聚合边界如何变化；非法状态在哪里被拒绝 | ORM/Table Model、DTO、持久化映射；事务编排、权限和重试 |
| Use Case | 应用动作和业务流程编排；Application Service、Command/Query Handler、用例实现 | 在什么条件下调用 Domain 与 Port；成功/失败条件、顺序、事务、权限、幂等、并发和异常处理 | 纯领域计算；HTTP/消息协议转换；数据库技术实现 |
| Entry | 外部触发及可观察契约；Controller、Resolver、Message Listener、Scheduler、CLI、API/Event DTO 和驱动端 Adapter | 请求、响应、事件或任务如何转换为用例调用；校验、认证、错误映射和兼容性 | 核心业务规则；被驱动端 Port；持久化实现 |

Data 只描述持久化结构和约束，Adapter 才描述数据操作以及持久化结构到核心类型的转换。Domain 不等同于 `Entity.java · Entity#method`，也不应负责聚合数据访问。Port 的归属由核心所需契约决定，不由目录名称决定。

2. 按“相同触发 + 相同核心状态变化 + 相同可观察结果”划分最小业务流。共用 Repository 或工具类不足以归为同一流；业务意图分化时拆分。
3. 分别确定每条业务流的“底”：选择该流中包含 `[变更]` 节点、且能够解释变化来源的最低证据类型，而不是选择仓库中理论上可能存在的最低架构层。
   - 修改迁移、DDL、字段或约束时，从 Data 开始。
   - 只修改 SQL、Mapper、Repository 实现、缓存或外部系统适配时，从 Adapter 开始。
   - 只修改 Repository/Gateway 接口或应用边界契约时，从 Port 开始。
   - 只修改实体、值对象、领域规则、计算或状态机时，从 Domain 开始。
   - 只修改事务脚本、调用顺序、权限、幂等或并发编排时，从 Use Case 开始。
   - 只修改 API 字段、响应、事件或任务入口，且没有更低层行为变化时，从 Entry 开始。

   起点以下没有参与本次行为变化的类型不展示，也不写 `未涉及`。如果高层变更声称会落到更低层状态，例如新增请求字段应写入实体或数据库，应反向检查其直接映射和写入落点；未连接时报告断链，不为其虚构更低层变更。
4. 从起点只沿该变化对业务语义的实际传导路径向上追踪。`Data → Adapter → Port → Domain → Use Case → Entry` 是可能出现的证据类型，不是固定步骤、运行时调用顺序或源码依赖方向。最终输出应收敛为四类关键检查点：

   - 起点事实：最底层实际发生了什么，系统现在允许、拒绝、保存、读取或表达什么。
   - 语义承接：哪个映射、契约、规则或分支首先赋予该事实领域或应用含义。
   - 用例行为：成功条件、失败条件、状态转换、事务边界或关键调用如何变化。
   - 顶层业务：外部行为或业务流程最终如何变化，是否符合任务目标与业务设计。

   一个检查点可以有多行，但每行只记录一个可独立定位和核查的符号。证据节点使用 `path:line`、`Class#method` 或 `table.column`，并标记证据类型，例如 `[变更][Data]`、`[承接][Domain]`。不要把多个文件名放在一个证据节点后只给出笼统结论。

证据节点分为两类：

- `[变更]`：符号本身位于 diff 中，必须写明 `before → after`。Schema 变化应逐项列出字段或约束，不能只写迁移文件名。
- `[承接]`：符号本身未修改，但它读取、映射、组装或调用了变更结果，是证明层间传导所必需的最小上下文。具体变化写“未改动”，然后说明它承接了哪个变更输入；不要将其伪装成本次改动。

每一行的“向上传导”必须指出本检查点的事实被后续哪个符号解释、转换、调用或暴露。后续输入应能对应前一项产出；连接不上时应拆分业务流或标记 `[无法确认]`。

只保留改变业务语义或证明语义连续性的节点。纯 DTO 转换、无语义 Mapper 转发和接口机械透传可以合并为一句“承接无语义变化”；但当字段漏传、转换错误、查询条件、锁或返回契约可能改变结论时，必须展开。某个节点已经足以证明下一步行为时停止深入实现。重点检查 `DB constraint → Domain invariant`、`Port contract → Use Case decision`、`Domain state → externally visible behavior` 等语义跃迁。没有显式 Port、独立 Domain 行为或 Use Case 边界时直接省略，不强行套用 DDD。

5. 对照每项明确目标，只报告有具体位置且影响正确性、目标、数据完整性或重要可观察契约的问题。忽略格式偏好和空泛最佳实践。

## 输出

```text
结论：<达成 | 部分达成 | 未达成 | 无法确认> — <一句话原因>
范围：<版本范围或工作区；含不确定性>

### Bottom-up 业务链审阅

变更链：<触发—核心状态变化—结果>
起点：<Data | Adapter | Port | Domain | Use Case | Entry> — <本次业务流最低且能够解释变化来源的检查点>

| 检查点 | 证据节点 | 关键变化 | 业务语义 | 向上传导 |
| --- | --- | --- | --- | --- |
| 起点事实 | `[变更][Data/Adapter/Port/Domain/Use Case/Entry] file:line · symbol` | 最底层实际发生了什么 `before → after` | 系统底层现在允许、拒绝、保存、读取或表达什么 | 哪个上层规则首先因此受到影响 |
| 语义承接 | `[变更/承接][证据类型] file:line · symbol` | 仅记录改变业务含义的映射、契约、规则或分支；纯透传可压缩 | 底层事实被解释成什么领域或应用语义 | 最终影响哪个 Use Case 或业务决策 |
| 用例行为 | `[变更/承接][Use Case/Domain/Port] file:line · symbol` | 成功条件、失败条件、状态转换、事务边界或关键调用发生什么变化 | 用户动作现在会得到什么不同结果 | 由哪个 Entry 暴露给外部 |
| 顶层业务 | `[变更/承接][Entry/Use Case] file:line · symbol` | 外部行为或业务流程最终发生什么变化 | 是否符合本次任务要求和业务设计 | 得出完成、缺口或设计冲突结论 |

业务影响：
- <用“底层事实 → 关键语义 → 用例行为 → 顶层业务结果”总结最终结果、适用条件和失败边界，不重复逐层代码事实>

目标核对：
- <目标>：<证据或缺口> → <结论>

风险：
- [阻断 | 重要 | 一般] `path:line`：<问题>；影响：<后果>；建议：<最小修正>
```

期望的链条形态类似：

```text
inventory_lot 不再允许 STATUS 类型
→ STATUS 库存不再具有批次语义
→ 库存更新用例改走 StockState
→ STATUS 食材只暴露 ENOUGH/NORMAL/LOW/EMPTY
→ 符合目标；如果 Use Case 仍创建 Lot，则底层实现没有真正支撑业务设计
```

没有明确目标时，说明无法判定目标是否达成，但仍可审阅行为。没有具体风险时写“未发现会阻断目标的具体问题”。风险按实际影响排序。

结论规则：目标均有证据且无阻断问题为 `达成`；存在目标缺口或重要边界未覆盖为 `部分达成`；行为与目标冲突或核心路径失效为 `未达成`；范围、目标、契约或运行时事实不足为 `无法确认`。

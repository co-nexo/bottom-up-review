interface:
  display_name: "Bottom-up Review"
  short_description: "从 Git 差异自底向上定位业务变化并核对任务目标"
  default_prompt: "Use $bottom-up-review to trace the current Git diff upward from changed code and verify the stated business goal."
